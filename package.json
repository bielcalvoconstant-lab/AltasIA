"dependencies": {
  "lucide-react": "^0.511.0",
  "next": "16.2.9",
  "react": "^19.0.0",
  "react-dom": "^19.0.0",
  "@auth/prisma-adapter": "^2.7.0",
  "next-auth": "5.0.0-beta.25",
  "@prisma/client": "^6.0.0",
  "tailwind-merge": "^2.5.0",           // ← Adiciona isso
  "clsx": "^2.1.1",                      // ← Adiciona isso (usado junto com tailwind-merge)
  "sonner": "^1.5.0"                     // ← (se estiver usando toast notifications)
},
"devDependencies": {
  "prisma": "^6.0.0",
  "tailwindcss": "^4.0.0",
  "typescript": "^5.8.0",
  "postcss": "^8.4.0",                   // ← Adiciona isso (Tailwind precisa)
  "autoprefixer": "^10.4.0"              // ← Adiciona isso (Tailwind precisa)
}
